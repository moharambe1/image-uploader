import rmt_datasource from "./remote-datasources";

const dmn_repositories = {
  uploadImg: rmt_datasource.uploadImg,
  getLink: rmt_datasource.getLink,
  getImg: rmt_datasource.getImg,
};

export default dmn_repositories;
